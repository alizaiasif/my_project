<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="form-group">
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location,class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :Room_No %><br>
    <%= f.number_field :Room_No,class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :room_type_id %><br>
    <%= f.text_field :room_type_id,class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
</div>
<% end %>
